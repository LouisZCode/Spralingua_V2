import type { Metadata } from "next";
import Bauteil from "@/components/Bauteil";

export const metadata: Metadata = {
  title: "Spralingua — Declension Practice",
};

export default function BauteilPage() {
  return <Bauteil />;
}
