import type { Metadata } from "next";
import Faelle from "@/components/Faelle";

export const metadata: Metadata = {
  title: "Spralingua — Fälle",
};

export default function FaellePage() {
  return <Faelle />;
}
